{
  "name": "intermappler",
  "version": "1.0.0",
  "description": "Sistema de traducción para InterMappler",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "echo \"No linting required\" && exit 0",
    "build": "echo \"Build complete\" && exit 0"
  },
  "keywords": ["translation", "i18n", "multilanguage", "intermappler"],
  "author": "InterMappler Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}